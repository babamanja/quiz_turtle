# Local Prisma / backend — point at Docker Postgres (see docker-compose.yml).
DATABASE_URL=postgresql://prisma:prisma@localhost:15432/language_turtle

# Telegram bot token from @BotFather (required to run the bot).
TG_BOT_TOKEN=
